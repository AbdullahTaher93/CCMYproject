dist: trusty
language: node_js
sudo: false
node_js:
  - v11
install:
  - npm install mocha
  - npm install

 before_script:
  - cd $TRAVIS_BUILD_DIR
  - composer install

script:
  - $TRAVIS_BUILD_DIR/bin/phpcs --config-set installed_paths vendor/drupal/coder/coder_sniffer
  - $TRAVIS_BUILD_DIR/bin/phpcs -p --report=full $TRAVIS_BUILD_DIR/sites/all/modules/custom
  - npm test